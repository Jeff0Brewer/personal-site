import * as React from "react"

// markup
const NotFoundPage = () => {
  return (
    <main style={{
      color: "#fff",
      padding: "50px",
      fontFamily: "Arial, Helvetica, sans-serif",
      fontSize: "100px"
    }}>
      404
    </main>
  )
}

export default NotFoundPage
